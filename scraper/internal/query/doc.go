// Package query handles incoming search requests: language detection,
// Devanagari normalization, admin geo-code filter construction, and the
// stage-1 BM25 candidate fetch against OpenSearch. Owned by Person 5 — see
// docs/TASK-SPLIT-search-engine-scraper.md.
package query
