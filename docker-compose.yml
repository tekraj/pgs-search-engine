services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pgs-api

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    restart: unless-stopped

    command: >
      uvicorn api.main:app
      --host 0.0.0.0
      --port 8000
      --reload